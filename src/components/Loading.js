import React from 'react'
import loading from '../loading.gif'

const style = {
  height: '200px',
  margin: '0 auto',
  display: 'block'
}
const Loading = () => (<img style={style} alt="loading" src={loading}/>)

export default Loading